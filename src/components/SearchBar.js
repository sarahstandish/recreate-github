import React from "react";
import "./SearchBar.css";

const SearchBar = () => {
  return (
    <input type="text" value="Search Github" className="searchbar"></input>
  );
};

export default SearchBar;
